# URL base de la API del backend (incluye /api al final).
# Desarrollo local:
VITE_API_URL="http://localhost:3000/api"

# Despliegue en red local / VM Ubuntu: usa la IP del servidor donde corre el backend.
# VITE_API_URL="http://192.168.100.20:3000/api"
